import WrapperLayout from '../../hocs/WrapperLayout';

const contacts = props => {
  return (
    <section>
      <WrapperLayout>
        <h2>Контакты</h2>
      </WrapperLayout>
    </section>
  );
};

export default contacts;